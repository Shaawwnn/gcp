{
  "name": "@gcp-learning/shared",
  "version": "1.0.0",
  "description": "Shared types and constants for GCP learning project",
  "main": "dist/types/index.js",
  "types": "dist/types/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist types/**/*.js types/**/*.d.ts types/**/*.js.map constants/**/*.js constants/**/*.d.ts constants/**/*.js.map",
    "lint": "echo 'No linting configured for shared package'"
  },
  "keywords": [
    "shared",
    "types",
    "constants"
  ],
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "license": "MIT",
  "private": true
}
